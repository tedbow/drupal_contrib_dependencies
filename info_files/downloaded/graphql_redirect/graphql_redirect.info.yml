name: GraphQL Redirect
type: module
description: 'Overrides Route field behavior so that it takes redirects into account.'
package: GraphQL (Experimental)
core: 8.x
dependencies:
  - graphql_core
  - redirect
