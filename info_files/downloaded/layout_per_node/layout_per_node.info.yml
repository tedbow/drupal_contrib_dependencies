name: 'Layout Per Node'
description: 'Set layout, and position fields and blocks on a per-node basis.'
package: Layout

type: module
core: 8.x

dependencies:
  - field_layout:field_layout

configure: layout_per_node.settings

php: 5.5.9
